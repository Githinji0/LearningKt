fun main() {
    //a companion object is a special object which is bound to (i.e. is a part of) a class definition. 
    var result:Int = Calculator.sum(10,15)
    println(result)
    Calculator.max()
}
