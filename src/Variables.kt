fun main(){
    var firstName = "william "
    var secondName = "Doe"

    var bothNames = firstName + secondName

    println(bothNames)
    //you can also add values
    // we use the camelCase system to name variables
}